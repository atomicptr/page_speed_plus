plugin.tx_pagespeed_plus {
    settings {
        http2 {
            serverPushEnable = {$plugin.tx_pagespeed_plus.settings.http2.serverPushEnable}
            maxHeaderLength = {$plugin.tx_pagespeed_plus.settings.http2.maxHeaderLength}
        }

        htmlCompress {
            enable = {$plugin.tx_pagespeed_plus.settings.htmlCompress.enable}
        }
    }
}